NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_API_URL=http://localhost:8000

# Revalidation à la demande (app/api/revalidate/route.ts) — MÊME valeur que
# REVALIDATE_SECRET côté backend. Pas de préfixe NEXT_PUBLIC_ : ne doit jamais
# être exposé au navigateur, uniquement lu côté serveur (Route Handler).
REVALIDATE_SECRET=
